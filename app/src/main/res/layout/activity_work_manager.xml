<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fadeScrollbars="false"
    android:fitsSystemWindows="true"
    android:padding="@dimen/screen_padding">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="vertical">

        <Button
            android:id="@+id/simpleWorkerId"
            style="@style/launchBtnStyle"
            android:text="@string/simple_worker" />

        <Button
            android:id="@+id/workManagerId"
            style="@style/launchBtnStyle"
            android:text="Example" />

        <Button
            android:id="@+id/pagingFromLocalRemoteApiId"
            style="@style/launchBtnStyle"
            android:text="@string/paging_using_remote_and_local"
            android:visibility="gone" />

    </LinearLayout>
</ScrollView>
